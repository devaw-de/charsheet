export enum LevelUpStrategy {
  MAX = 'Max value',
  AVG = 'Average value',
  ROLL = 'Die roll'
}

export enum Settings {
  DISTANCES = 'DISTANCES'
}

export enum Distances {
  FEET = 'FEET',
  METERS = 'METERS'
}
